<Project>
  <PropertyGroup>
    <!-- Redirect bin/ and obj/ to a folder outside the Unity project -->
    <BaseOutputPath>$(SolutionDir)..\VSBuildOutput\</BaseOutputPath>
    <BaseIntermediateOutputPath>$(SolutionDir)..\VSBuildObj\</BaseIntermediateOutputPath>
  </PropertyGroup>
</Project>